# Fixture notes

A Markdown file, present so the tests can prove that Rule B scans Markdown like any other
source file — planting a retired literal here must turn the gate red, exactly as planting one
in src/app.mjs does.
