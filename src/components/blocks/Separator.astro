---
/* core/separator. Markup observed from core's own save.js (src/separator/
 * save.js): `has-alpha-channel-opacity` is core's default `opacity`
 * value and appears unconditionally here for the same reason it appears unconditionally in
 * the markup contract table — this project sets no editor default that would turn it off.
 * No props: the contract type carries only `type`. */
---
<hr class="wp-block-separator has-alpha-channel-opacity" />
