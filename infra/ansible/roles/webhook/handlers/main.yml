---
- name: restart webhook
  ansible.builtin.systemd:
    name: "{{ webhook_service_name }}"
    daemon_reload: true
    state: restarted

- name: reload systemd daemon
  ansible.builtin.systemd:
    daemon_reload: true
